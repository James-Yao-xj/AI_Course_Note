#import "@preview/ctyp:0.3.0": ctyp
#let (ctypset, cjk) = ctyp()
#let (song, hei, kai, fang) = cjk
#show: ctypset

#set page(
  paper: "a4",
  margin: (top: 2cm, bottom: 2cm, left: 1cm, right: 1cm),
  header: context [
    #text(8pt, gray)[机器学习]\
  ],
  numbering: "1"
)

#set heading(numbering: "1.")

#align(center)[
  #text(size: 24pt, weight: "bold")[第三讲]\
  #v(10pt)
  #text(size: 16pt)[——机器学习介绍与线性模型——]
]

= 机器学习

== 什么是机器学习？

*定义*：机器学习是指算法的设计与分析，使得模型能够*基于经验*，在*某些任务*上，其*表现*不断提升。

也就是说，机器学习的三个核心元素是：

- *任务（Task）*：我们希望模型完成什么事情；
- *经验（Experience）*：模型可利用的数据或历史样本；
- *表现（Performance）*：衡量模型做得好不好的标准。

机器学习的本质可以概括为：

#align(center)[
  数据 $arrow$ 学习算法 $arrow$ 知识/预测规则
]

或者说，是一个“从数据到知识”的过程。

== 大数据时代的到来

随着互联网、社交网络、传感器、移动设备等的发展，数据量爆炸式增长。课件中指出，人类每天产生的数据量可达到：

#align(center)[
  $2.5 times 10^18$
]

字节级别的数据为机器学习提供了丰富的“经验”，也推动了从数据中自动提取规律的需求。

因此，机器学习成为连接“*数据*”与“*知识*”的重要工具。

== 机器学习的直观理解

人类学习和机器学习有相似之处：

- 人类通过观察大量例子积累经验；
- 机器通过训练数据学习规律；
- 最终都形成某种“判断能力”或“预测能力”。

例如：

- 看到许多猫和狗的图片后，学会区分“猫”与“狗”；
- 阅读很多文本后，学会判断一篇文章属于体育、新闻还是科技；
- 观察历史房价后，学会预测未来房价。

== 机器学习的典型应用

机器学习已经广泛应用于许多场景中，例如：

- 图片分类；
- 文档分类；
- 垃圾邮件过滤；
- 股市预测；
- 语音识别；
- 自然语言处理；
- 计算机视觉；
- 机器人；
- 医疗图像分析；
- 计算生物学；
- 社交网络分析等。

近年来，机器学习也掀起了新一轮 AI 热潮，例如大语言模型、自动驾驶、生成式 AI 等。

== 机器学习的局限性

尽管机器学习取得了巨大成功，但距离真正像人类一样智能仍有较大差距。

当前机器学习系统通常仍然：

- 难以自动适应开放世界中的新场景；
- 难以识别训练中从未见过的新种类；
- 容易依赖训练数据分布，泛化能力有限。

因此，机器学习还有很长的路要走。

== 任务、经验、表现

前面提到，理解机器学习可以抓住三个关键词：

- *任务*；
- *经验*；
- *表现*。

下面分别展开说明。

== 机器学习的任务

机器学习的任务可以分为以下几类：

- *有监督学习*；
- *无监督学习*；
- *半监督学习*；
- *弱监督学习*；
- *强化学习*。

=== 有监督学习

有监督学习中，训练数据包含输入和标签，即：

#align(center)[
  给定 $x in cal(X)$，学习预测 $y in cal(Y)$
]

其中：

- $x$ 表示输入数据；
- $y$ 表示标签或目标值。

根据标签类型不同，有监督学习又可分为：

- *分类问题*：标签离散；
- *回归问题*：标签连续。

*分类问题* 示例：

- 图片分类：猫 / 狗；
- 文档分类：体育 / 科技 / 新闻；
- 垃圾邮件识别：Spam / Not Spam；
- 医疗图像诊断：正常 / 异常。

*回归问题* 示例：

- 房价预测；
- 销量预测；
- 气温预测；
- 股票价格预测。

=== 无监督学习

无监督学习中，训练数据只有输入，没有标签，即：

#align(center)[
  给定 $x in cal(X)$，在没有 $y$ 的情况下学习数据中的结构
]

无监督学习可理解为“没有老师的学习”或“自己教自己学习”。

典型任务包括：

- *密度估计*：估计数据分布；
- *聚类*：将相似的数据聚到一起；
- *降维*：用更少的变量表示原始数据。

*聚类* 的目标是把相似样本划分为同一组。  
*降维* 的目标是从高维数据中提取低维表示，便于可视化、压缩或后续学习。

=== 其他学习范式

除了监督学习和无监督学习，还有：

- *半监督学习*：少量有标签数据 + 大量无标签数据；
- *弱监督学习*：标签不精确、不完整或含噪声；
- *强化学习*：智能体通过与环境交互，根据奖励信号学习策略。

== 机器学习的经验

在机器学习中，经验通常由*训练数据*提供。

需要区分：

- *训练数据（training data）*：用于学习模型；
- *测试数据（test data）*：用于评估模型在新样本上的表现。

一个好的机器学习算法应当：

- 不会对训练数据发生*过拟合（overfit）*；
- 能够对测试数据良好*泛化（generalize）*。

也就是说，模型不仅要“记住”训练样本，更要学到背后的规律。

== 机器学习的表现

机器学习中的“表现”通常通过*损失函数（loss function）*或评价指标衡量。

设真实标签为 $Y$，模型预测为 $f(X)$，则表现可用它们之间的接近程度衡量。

常见例子：

- *二分类*中，可用 $0/1$ 损失：
  $
    "loss"(Y, f(X)) = 1_(f(X) != Y)
  $

- *回归*中，常用平方损失：
  $
    "loss"(Y, f(X)) = (f(X) - Y)^2
  $

- *密度估计*中，常用负对数似然损失。

因此，机器学习的目标通常可以表述为：

#align(center)[
  寻找一个预测函数 $f$，使得平均损失尽可能小
]

== 机器学习 vs. 优化

从数学角度看，机器学习通常表现为一个优化问题。

理想目标是寻找预测规则 $f$，使其在整体数据分布上最优，即最小化期望风险：

$
f^* = arg min_f EE_(X,Y)["loss"(Y, f(X))]
$

但是，真实数据分布通常未知，因此无法直接优化期望风险。  
于是我们使用训练数据上的经验风险来近似：

$
hat(f)_n = arg min_f (1 / n) sum_(i=1)^n "loss"(y_i, f(x_i))
$

这体现了机器学习与优化的关系：

- *优化*提供求解工具；
- *机器学习*关注如何从有限数据中学到可泛化的规律。

== 机器学习 vs. 统计

机器学习与统计学关系密切。

二者都关心如何从数据中推断规律，但机器学习更强调：

- 预测性能；
- 算法设计；
- 大规模计算；
- 泛化能力。

因此，机器学习不只是简单的曲线拟合，而是要构建适用于未来测试数据的预测规则。

== 小结

本部分主要介绍了：

- 机器学习的定义；
- 三个基本元素：任务、经验、表现；
- 常见学习范式；
- 训练/测试数据、过拟合与泛化；
- 机器学习与优化、统计之间的联系。

= 线性模型

== 线性模型的基本思想

线性模型是机器学习中最基础、最重要的一类模型。

其核心思想是：  
*通过输入特征的线性组合来预测输出。*

设输入特征为 $x = (x_1, x_2, dots, x_p)$，则线性模型写为：

$
f(x) = w^T x + b = w_1 x_1 + w_2 x_2 + dots + w_p x_p + b
$

其中：

- $w = (w_1, w_2, dots, w_p)$ 为权重参数；
- $b$ 为偏置项；
- $w, b$ 都是需要通过数据学习得到的模型参数。

例如在房价预测中，输入特征可以包括：

- 建造年份；
- 房屋面积；
- 楼层数；
- 房屋质量评分；
- 卧室数量等。

线性模型会为每个特征分配一个权重，再将它们加权求和得到预测值。

== 线性模型的任务类型

线性模型可以用于两类基本任务：

- *线性回归*：预测连续数值；
- *线性分类*：预测离散类别。

例如：

- 房价预测属于回归问题；
- 股票涨跌判断属于分类问题。

== 线性回归

=== 线性回归模型

给定数据集

$
D = \{(x_1, y_1), (x_2, y_2), dots, (x_n, y_n)\}
$

线性回归希望学习一个模型：

$
f(x_i) = w^T x_i + b
$

使得预测值 $f(x_i)$ 尽可能接近真实值 $y_i$。

=== 目标函数

线性回归中最常用的目标函数是*最小二乘（least squares）*，即最小化平方误差和：

$
(w^*, b^*) = arg min_(w,b) (1 / n) sum_(i=1)^n (y_i - w^T x_i - b)^2
$

这就是*最小二乘估计*。

选择平方损失的原因包括：

- 数学形式简单；
- 对大误差惩罚更强；
- 通常可以得到良好的解析性质。

=== 经验风险与大数定理

理想情况下，我们想最小化总体上的期望平方误差：

$
f^* = arg min_f EE[(f(X) - Y)^2]
$

但真实分布未知，因此用样本均值近似期望：

$
hat(f)_n = arg min_f (1 / n) sum_(i=1)^n (f(x_i) - y_i)^2
$

这背后依赖大数定理：当样本数足够大时，样本平均会逼近期望。

== 线性回归的矩阵形式

为方便表达，把参数和数据写成矩阵形式。

令

$
beta = [w_1, w_2, dots, w_p, b]^T
$

并构造设计矩阵 $A$，则目标函数可写为：

$
hat(beta) = arg min_beta (1 / n) (A beta - Y)^T (A beta - Y)
$

其中：

- $A$ 是 $n times p$ 或扩展后的设计矩阵；
- $Y$ 是标签向量；
- $beta$ 是待估计参数向量。

这样可以把回归问题统一成标准的二次优化问题。

== 线性回归的闭式解

=== 正规方程

对目标函数求导并令其为零，可得到正规方程：

$
A^T A hat(beta) = A^T Y
$

若 $A^T A$ 可逆，则可得到闭式解：

$
hat(beta) = (A^T A)^(-1) A^T Y
$

这就是线性回归的解析解。

=== 闭式解存在的条件

闭式解存在的关键条件是 $A^T A$ 可逆。  
这通常要求：

- 特征之间线性无关；
- 设计矩阵满列秩。

特别地，当样本数 $n < p$ 时，通常有：

$
"rank"(A^T A) <= min(n, p) < p
$

因此 $A^T A$ 不可逆，闭式解不存在。

此外，即使理论上可逆，当特征维度很高时，直接求逆在计算上也会非常昂贵。

== 梯度下降

当闭式解不存在，或者矩阵求逆计算代价过大时，可以将线性回归作为优化问题，用*梯度下降（gradient descent）*求解。

设目标函数为：

$
J(beta) = (A beta - Y)^T (A beta - Y)
$

由于这是一个凸函数，因此可沿梯度负方向迭代更新参数：

$
beta^(t+1) = beta^t - alpha nabla J(beta^t)
$

其中：

- $beta^t$ 为第 $t$ 次迭代的参数；
- $alpha$ 为步长（学习率）；
- $nabla J(beta^t)$ 为当前梯度。

=== 步长的影响

步长 $alpha$ 的取值对收敛效果有重要影响：

- $alpha$ 过大：收敛快，但可能震荡甚至发散；
- $alpha$ 过小：收敛稳定，但速度较慢。

因此需要合理选择学习率。

== 正则化思想

当特征维度高、样本较少，或者特征之间存在相关性时，普通最小二乘容易出现：

- $A^T A$ 不可逆；
- 模型参数过大；
- 过拟合。

为了解决这些问题，可以在目标函数中加入*正则化项（regularization term）*。

正则化的思想是：

- 在拟合训练数据的同时；
- 对模型复杂度加以约束；
- 从而提高泛化能力。

== 从贝叶斯观点理解正则化

从贝叶斯观点看，可以为参数 $beta$ 引入先验分布，然后做 MAP（最大后验估计）：

$
hat(beta)_("MAP") = arg max_beta log p(D | beta) + log p(beta)
$

其中：

- $log p(D | beta)$ 是似然项；
- $log p(beta)$ 是先验项。

不同先验会对应不同形式的正则化。

== Ridge 回归

若对参数引入高斯先验：

$
beta ~ cal(N)(0, tau^2 I)
$

则 MAP 估计等价于求解：

$
hat(beta) = arg min_beta sum_(i=1)^n (y_i - x_i^T beta)^2 + lambda ||beta||_2^2
$

这就是 *Ridge Regression*（岭回归）。

其中 $||beta||_2^2$ 是 $L_2$ 正则项。

=== Ridge 回归的特点

- 惩罚较大的参数；
- 使参数整体变小、更平滑；
- 通常不会把参数精确压到 0；
- 可缓解共线性问题；
- 对应解为：

$
hat(beta) = (A^T A + lambda I)^(-1) A^T Y
$

由于加入了 $lambda I$，矩阵更容易可逆，因此能解决普通最小二乘不可逆的问题。

== Lasso 回归

若对参数引入拉普拉斯先验：

$
p(beta) "propto" e^(-|beta|/t)
$

则 MAP 估计等价于：

$
hat(beta) = arg min_beta sum_(i=1)^n (y_i - x_i^T beta)^2 + lambda ||beta||_1
$

这就是 *Lasso Regression*。

其中 $||beta||_1$ 是 $L_1$ 正则项。

=== Lasso 回归的特点

- 能产生*稀疏解*；
- 即很多参数会被直接压缩为 0；
- 相当于自动进行特征选择；
- 在高维问题中尤其有用。

== $L_1$ 与 $L_2$ 正则化的比较

=== $L_2$ 正则化

$L_2$ 正则化对应圆形约束集，倾向于让所有参数都较小，但通常不会产生很多精确的 0。

优点：

- 解稳定；
- 数值上容易处理；
- 对共线性较鲁棒。

=== $L_1$ 正则化

$L_1$ 正则化对应菱形约束集，最优解更容易落在坐标轴上，因此容易得到稀疏解。

优点：

- 自动进行特征选择；
- 降低模型存储和计算成本；
- 适合高维稀疏问题。

=== 对比总结

- *Ridge*：参数变小，但通常不为 0；
- *Lasso*：容易得到稀疏解，部分参数直接为 0。

== 广义线性模型的思想

线性模型虽然简单，但形式非常灵活。

例如，对输出取对数，可得到 *log-linear regression*：

$
log y = w^T x + b
$

进一步地，可以考虑一个单调可微函数 $g(.)$，构造更一般的模型：

$
y = g^(-1)(w^T x + b)
$

这就是广义线性模型的基本思想。

它说明：

- 线性模型不一定只能直接预测 $y$；
- 也可以先对线性组合进行非线性变换，再得到输出。

== 逻辑回归

在线性模型中，分类问题常见的模型是*逻辑回归（logistic regression）*。

其形式是在 $w^T x + b$ 外层加上 Sigmoid 函数：

$
f_(w,b)(x) = sigma(w^T x + b)
$

其中：

$
sigma(z) = 1 / (1 + e^(-z))
$

逻辑回归输出的是一个介于 0 和 1 之间的概率，因此适合二分类问题。

虽然名字叫“回归”，但它本质上是一个分类模型。

== 实践流程：以房价预测为例

实际机器学习任务通常包括以下步骤：

1. *问题形式化*：明确预测目标；
2. *数据获取*：收集历史房屋信息及售价；
3. *数据预处理*：清洗数据、构造特征；
4. *建模与调参*：训练模型并选择超参数；
5. *系统部署*：将模型用于在线预测；
6. *持续维护*：监控效果，用新数据更新模型。

这说明机器学习不仅仅是“选一个模型”，而是一个完整的工程流程。

= 本节总结

本节课主要介绍了机器学习和线性模型的基本概念。

== 机器学习部分

- 机器学习是基于经验提升任务表现的算法设计与分析；
- 三个核心要素是：任务、经验、表现；
- 机器学习可分为有监督学习、无监督学习、半监督学习、弱监督学习和强化学习；
- 好的模型应避免过拟合，并具有良好的泛化能力；
- 机器学习与优化、统计密切相关。

== 线性模型部分

- 线性模型通过特征的线性组合进行预测；
- 线性回归用于连续值预测；
- 最小二乘是线性回归的经典目标函数；
- 当 $A^T A$ 可逆时，线性回归有闭式解；
- 当不可逆或维度过高时，可用梯度下降或正则化方法；
- Ridge 使用 $L_2$ 正则，Lasso 使用 $L_1$ 正则；
- Lasso 更容易产生稀疏解；
- 广义线性模型与逻辑回归扩展了线性模型的应用范围。

= 思考题

- 机器学习的三个基本元素分别是什么？
- 有监督学习与无监督学习的区别是什么？
- 什么是过拟合？什么是泛化？
- 线性模型的基本形式是什么？
- 线性回归为什么常采用平方损失？
- 线性回归何时存在闭式解？
- Ridge 与 Lasso 的差别是什么？
- 为什么说 Lasso 具有特征选择作用？
- 逻辑回归为什么属于分类模型？